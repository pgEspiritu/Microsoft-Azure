## 81. You have a virtual machine named VM1 that runs Windows Server 2016. VM1 is in the East US Azure region. Which Azure service should you use from the Azure portal to view service failure notifications that can affect the availability of VM1?

- Azure Service Fabric.
- Azure Monitor.
- Azure virtual machines.
- Azure Advisor.

**Answer:** Azure virtual machines.

**Explanation:** The **Azure virtual machines** service in the Azure portal provides access to the management and status information for your virtual machines, including notifications and alerts related to issues that may affect the availability of the VM.

For this question, because the requirement specifically asks about **service failure notifications that can affect VM1**, the expected Azure portal service is **Azure virtual machines**.

**Why the others are incorrect:**
- **Azure Service Fabric** — A platform for building and managing distributed applications; it is not used to manage VM availability notifications.
- **Azure Monitor** — Used for monitoring metrics, logs, performance, and alerts, but it is not the expected answer for this specific Azure fundamentals question.
- **Azure Advisor** — Provides recommendations related to cost, security, reliability, performance, and operational excellence.

**Exam tip:** For questions specifically asking about the **status or notifications affecting an Azure VM**, look at the **Azure virtual machines** service in the Azure portal.

---

## 82. An Azure administrator plans to run a PowerShell script that creates Azure resources. You need to recommend which computer configuration to use to run the script. Solution: Run the script from a computer that runs Linux and has the Azure CLI tools installed. Does this meet the goal?

- Yes.
- No.

**Answer:** No.

**Explanation:** The solution does **not** meet the goal because the **Azure CLI** and **PowerShell** are different command-line tools.

Installing the Azure CLI on a Linux computer does not mean that a **PowerShell script** can be executed using the Azure CLI. To run a PowerShell script, the computer needs to have **PowerShell** installed.

Linux can run PowerShell, but the scenario specifically states that the computer has only the **Azure CLI tools** installed.

**Why the answer is No:**
- **Azure CLI** → Used to run Azure CLI commands.
- **PowerShell** → Required to execute PowerShell scripts.
- **Linux + Azure CLI only** → Cannot directly execute the PowerShell script because PowerShell is not installed.

**Exam tip:** Remember that **Azure CLI and Azure PowerShell are separate tools**. If the question says **PowerShell script**, make sure **PowerShell** is available on the computer.

---

## 83. An Azure administrator plans to run a PowerShell script that creates Azure resources. You need to recommend which computer configuration to use to run the script. Solution: Run the script from a computer that runs Chrome OS and uses Azure Cloud Shell. Does this meet the goal?

- Yes.
- No.

**Answer:** Yes.

**Explanation:** **Azure Cloud Shell** is a browser-based, interactive shell provided by Azure. It can be accessed from a web browser, so the underlying operating system of the computer does not need to be Windows.

Azure Cloud Shell provides **PowerShell** as one of its available shell environments. Therefore, an administrator using a **Chrome OS** computer can open Azure Cloud Shell in a browser and run the PowerShell script to create Azure resources.

**Why the answer is Yes:**
- **Chrome OS** → Can access Azure Cloud Shell through a web browser.
- **Azure Cloud Shell** → Provides a **PowerShell** environment.
- **PowerShell script** → Can be executed within the Cloud Shell PowerShell environment.
- **Azure resources** → Can be created and managed from Cloud Shell.

**Exam tip:** If the question involves **PowerShell + a device with an unsupported/limited operating system**, remember that **Azure Cloud Shell is browser-based** and provides both **PowerShell and Azure CLI**.

---

## 84. You have an Azure environment that contains 10 virtual networks and 100 virtual machines You need to limit the amount of inbound traffic to all the Azure virtual network. What should you create?

- One application security group (ASG).
- 10 virtual network gateways.
- 10 Azure ExpressRoute circuits.
- One Azure firewall.

**Answer:** One Azure firewall.

**Explanation:** **Azure Firewall** is a managed, cloud-based network security service that can control and filter **inbound and outbound network traffic** across Azure resources. A single Azure Firewall can be used to protect multiple virtual networks when they are appropriately connected and configured.

**Why the others are incorrect:**
- **One application security group (ASG)** — ASGs help group virtual machines and define network security rules, but they are associated with network interfaces and are not a centralized firewall for multiple virtual networks.
- **10 virtual network gateways** — A VPN gateway or virtual network gateway is primarily used to connect Azure virtual networks to other networks or provide connectivity between networks. It is not the appropriate centralized solution for filtering inbound traffic to all 10 virtual networks.
- **10 Azure ExpressRoute circuits** — ExpressRoute provides private connectivity between on-premises networks and Azure. It is not primarily a traffic-filtering service.
- **One Azure Firewall** — Provides centralized network traffic filtering and can protect multiple virtual networks.

**Exam tip:** For **centralized filtering of network traffic across multiple Azure virtual networks**, think **Azure Firewall**.

---

## 85. You have an Azure environment that contains multiple Azure virtual machines. You plan to implement a solution that enables the client computers on your on-premises network to communicate to the Azure virtual machines. You need to recommend which Azure resources must be created for the planned solution. Which two Azure resources should you include in the recommendation?

- A virtual network gateway.
- A load balancer.
- An application gateway.
- A virtual network.
- A gateway subnet.

**Answer:** A virtual network gateway and a gateway subnet.

**Explanation:** To establish connectivity between an **on-premises network** and Azure, a **virtual network gateway** is required. The virtual network gateway must be deployed into a dedicated **gateway subnet** within the Azure virtual network.

**Why the answers are correct:**
- **Virtual network gateway** — Provides connectivity between the on-premises network and the Azure virtual network, typically using a site-to-site VPN.
- **Gateway subnet** — A dedicated subnet named `GatewaySubnet` is required for deploying the virtual network gateway.

**Why the others are incorrect:**
- **Load balancer** — Distributes network traffic across backend resources; it does not provide on-premises connectivity.
- **Application gateway** — Provides application-level traffic routing and web application functionality; it is not required for basic on-premises-to-Azure connectivity.
- **Virtual network** — The Azure VMs already reside in an Azure virtual network, so it is not the additional resource being asked for in this scenario.

**Exam tip:** For **on-premises → Azure VNet connectivity**, remember:

**On-premises network → Virtual Network Gateway → GatewaySubnet → Azure VNet → Azure VMs**

---

## 86. You have an Azure environment. You need to create a new Azure virtual machine from a tablet that runs the Android operating system. Solution: You use Bash in Azure Cloud Shell. Does this meet the goal?

- Yes.
- No.

**Answer:** Yes.

**Explanation:** **Azure Cloud Shell** is a browser-based command-line environment that allows you to manage Azure resources without installing Azure management tools on the local device.

Because Azure Cloud Shell provides a **Bash** environment and can be accessed through a web browser, you can use it from an **Android tablet** to run Azure CLI commands and create an Azure virtual machine.

**Why the answer is Yes:**
- **Android tablet** — Can access Azure Cloud Shell through a web browser.
- **Azure Cloud Shell** — Provides a browser-based Bash environment.
- **Azure CLI** — Available within the Bash environment and can be used to create Azure resources.
- **Virtual machine** — Can be created using Azure CLI commands from Cloud Shell.

**Exam tip:** If the device has limited ability to install Azure tools, remember that **Azure Cloud Shell is browser-based** and can be used to manage Azure resources from supported devices.

---

## 87. Your company plans to move several servers to Azure. The company compliance policy states that a server named FinServer must be on a separate network segment. You are evaluating which Azure services can be used to meet the compliance policy requirements. Which Azure solution should you recommend?

- A resource group for FinServer and another resource group for all the other servers.
- A virtual network for FinServer and another virtual network for all the other servers.
- A VPN for FinServer and a virtual network gateway for another server.
- One resource group for all the servers and a resource lock for FinServer.

**Answer:** A virtual network for FinServer and another virtual network for all the other servers.

**Explanation:** An Azure **virtual network (VNet)** provides network isolation and segmentation for Azure resources. By placing **FinServer in a separate virtual network** from the other servers, the company can meet the requirement that FinServer be on a separate network segment.

**Why the answer is correct:**
- **Separate virtual networks** provide network-level isolation.
- FinServer can be placed in its own VNet.
- The remaining servers can be placed in a separate VNet.
- Network connectivity between the VNets can be controlled using appropriate networking and security configurations.

**Why the others are incorrect:**
- **Separate resource groups** — Resource groups provide logical management and organization, but they do not create network segmentation.
- **VPN and virtual network gateway** — These are primarily used to establish network connectivity between networks, not simply to isolate a server on a separate network segment.
- **Resource lock** — Prevents accidental modification or deletion of resources; it does not provide network isolation.

**Exam tip:** For **network segmentation/isolation**, think **Virtual Network (VNet)**.  
For **logical organization**, think **Resource Group**.

---

## 88. You plan to map a network drive from several computers that run Windows 10 to Azure Storage. You need to create a storage solution in Azure for the planned mapped drive. What should you create?

- An Azure SQL database.
- A virtual machine data disk.
- A Files service in a storage account.
- A Blobs service in a storage account.

**Answer:** A Files service in a storage account.

**Explanation:** **Azure Files** provides fully managed file shares in Azure that can be accessed using the **SMB (Server Message Block)** protocol. Windows 10 computers can connect to an Azure file share and map it as a network drive.

This makes Azure Files the appropriate storage solution when users need a **shared network drive** accessible from multiple Windows computers.

**Why the others are incorrect:**
- **Azure SQL database** — Used for storing and managing relational database data, not files accessed through a mapped network drive.
- **Virtual machine data disk** — Provides storage for a specific virtual machine and is not designed to function as a shared network file share.
- **Blobs service** — Designed for object storage such as documents, images, videos, backups, and other unstructured data. It does not provide the standard SMB-based mapped-drive functionality required here.

**Exam tip:** For **Windows network drives → Azure Files → SMB**.

---

## 89. Your company plans to migrate all its network resources to Azure. You need to start the planning process by exploring Azure. What should you create first?

- A subscription.
- A resource group.
- A virtual network.
- A management group.

**Answer:** A subscription.

**Explanation:** An **Azure subscription** provides the fundamental boundary for Azure resources, billing, access control, and resource management. Before creating resource groups, virtual networks, or other Azure resources, you generally need an Azure subscription.

Once the subscription is available, you can create **resource groups**, **virtual networks**, and other resources within it.

**Why the others are incorrect:**
- **Resource group** — A logical container for managing related Azure resources. It is created within a subscription.
- **Virtual network** — Provides network connectivity and segmentation for Azure resources, but it must be created within a subscription.
- **Management group** — Used to organize and manage multiple Azure subscriptions; it is not required to begin using a single Azure subscription.

**Exam tip:** A common Azure hierarchy is:

**Management Group → Subscription → Resource Group → Resources**

---

## 90. You have an on-premises application that sends email notifications automatically based on a rule, You plan to migrate the application to Azure. You need to recommend a serverless computing solution for the application. What should you include in the recommendation?

- A web app.
- A server image in Azure Marketplace.
- A logic app.
- An API app.

**Answer:** A logic app.

**Explanation:** **Azure Logic Apps** is a serverless workflow automation service that allows you to automate processes and integrate applications and services without managing the underlying servers.

For an application that needs to **automatically send email notifications based on a rule**, Logic Apps can use triggers and actions to implement the workflow. For example, a trigger can detect an event or condition, and an email action can send the notification automatically.

**Why the others are incorrect:**
- **A web app** — Used to host web applications and APIs; it is not primarily a workflow automation service.
- **A server image in Azure Marketplace** — Used to deploy virtual machines or software appliances and requires infrastructure management.
- **An API app** — Used to host APIs and is not specifically designed for serverless workflow automation.

**Exam tip:** For **serverless + workflow automation + triggers/actions**, think **Azure Logic Apps**.

---

## 91. You plan to deploy a website to Azure. The website will be accessed by users worldwide and will host large video files. You need to recommend which Azure feature must be used to provide the best video playback experience. What should you recommend?

- An application gateway.
- An Azure ExpressRoute circuit.
- A content delivery network (CDN).
- An Azure Traffic Manager profile.

**Answer:** A content delivery network (CDN).

**Explanation:** An **Azure Content Delivery Network (CDN)** caches content, such as large video files, at geographically distributed edge locations. Users can retrieve the content from an edge location that is closer to them, reducing latency and improving the **video playback experience** for users around the world.

**Why the others are incorrect:**
- **Application Gateway** — Provides application-level traffic routing and features such as Web Application Firewall (WAF); it is not designed primarily for global content delivery.
- **Azure ExpressRoute** — Provides private, dedicated connectivity between on-premises networks and Azure. It is not intended to improve public website video delivery worldwide.
- **Azure Traffic Manager** — Provides DNS-based traffic distribution across endpoints, but it does not cache large video files at edge locations.

**Exam tip:**  
**Global users + large static media/video files + better performance → CDN**

---

## 92. Your company plans to deploy several million sensors that will upload data to Azure. You need to identify which Azure resources must be created to support the planned solution. Which two Azure resources should you identify?

- Azure Data Lake.
- Azure Queue storage.
- Azure File Storage.
- Azure IoT Hub.
- Azure Notification Hubs.

**Answer:** Azure IoT Hub and Azure Data Lake.

**Explanation:** A solution involving **millions of sensors sending data to Azure** requires a service designed specifically for large-scale IoT device connectivity and a scalable data store for the incoming data.

- **Azure IoT Hub** — Provides secure, reliable, bi-directional communication between IoT devices and Azure. It is designed to support large numbers of devices and handle high volumes of telemetry data.
- **Azure Data Lake** — Provides highly scalable storage for large volumes of data, making it suitable for storing and analyzing the telemetry generated by millions of sensors.

**Why the others are incorrect:**
- **Azure Queue Storage** — Designed for storing messages between application components, but it is not specifically designed for managing millions of IoT devices.
- **Azure File Storage** — Provides managed file shares and is not designed for IoT telemetry ingestion.
- **Azure Notification Hubs** — Designed primarily for sending push notifications to mobile devices, not for collecting sensor telemetry.

**Exam tip:**  
**Millions of sensors → IoT Hub**

**Massive amounts of telemetry/data → Data Lake**

---

## 93. You have an Azure web app. You need to manage the settings of the web app from an iPhone. What are two Azure management tools that you can use?

- Azure CLI.
- The Azure portal.
- Azure Cloud Shell.
- Windows PowerShell.
- Azure Storage Explorer.

**Answer:** The Azure portal and Azure Cloud Shell.

**Explanation:** Since the administrator is using an **iPhone**, the management tools should be accessible through a web browser without requiring local software installation.

- **The Azure portal** — A web-based graphical interface that can be accessed from an iPhone browser to manage Azure resources, including web apps.
- **Azure Cloud Shell** — A browser-based command-line environment that provides **Azure CLI** and **PowerShell**, allowing Azure resources to be managed from an iPhone.

**Why the others are incorrect:**
- **Azure CLI** — Although Azure CLI can manage Azure resources, it normally needs to be installed or accessed through an environment such as Cloud Shell. The iPhone itself is not the expected execution environment in this question.
- **Windows PowerShell** — Requires a suitable PowerShell environment and is not natively available as a local Windows-style shell on an iPhone.
- **Azure Storage Explorer** — A separate application primarily used to manage Azure Storage resources, rather than general Azure web app management.

**Exam tip:** For **Azure management from a mobile device**, think:

**Azure Portal + Azure Cloud Shell**

---

## 94. Company plans to deploy an Artificial Intelligence (AI) solution in Azure. What should the company use to build, test, and deploy predictive analytics solutions?

- Azure Logic Apps.
- Azure Machine Learning Studio.
- Azure Batch.
- Azure Cosmos DB.

**Answer:** Azure Machine Learning Studio.

**Explanation:** **Azure Machine Learning Studio** is designed to help data scientists and developers **build, train, test, and deploy machine learning and predictive analytics models**.

It provides tools for creating machine learning solutions, managing datasets and experiments, training models, and deploying trained models as services.

**Why the others are incorrect:**
- **Azure Logic Apps** — Used to automate workflows and integrate applications and services.
- **Azure Batch** — Used to run large-scale parallel and high-performance computing workloads.
- **Azure Cosmos DB** — A globally distributed database service designed for storing and querying data.

**Exam tip:**  
**Build + test + deploy predictive analytics / machine learning → Azure Machine Learning Studio**

---

## 95. You have an Azure subscription named Subscription1. You sign in to the Azure portal and create a resource group named RG1. From Azure documentation, you have the following command that creates a virtual machine named VM1. `az vm create --resource-group RG1 --name VM1 --image UbuntuLTS --generate-ssh-keys`. You need to create VM1 in Subscription1 by using the command. Solution: From the Azure portal, launch Azure Cloud Shell and select PowerShell. Run the command in Cloud Shell. Does this meet the goal?

- Yes.
- No.

**Answer:** No.

**Explanation:** The command provided is an **Azure CLI** command because it begins with `az vm create`.

If you launch **Azure Cloud Shell** and select **PowerShell**, you are using the PowerShell environment. To run the Azure CLI command as provided, you should select **Bash** in Azure Cloud Shell.

**Correct approach:**

1. Open **Azure Cloud Shell** from the Azure portal.
2. Select **Bash**.
3. Run the Azure CLI command:

```bash
az vm create --resource-group RG1 --name VM1 --image UbuntuLTS --generate-ssh-keys
```

**Why the answer is No:**

- `az vm` create → Azure CLI
- Azure CLI → Can be run in Bash
- PowerShell Cloud Shell → Intended for Azure PowerShell commands such as `New-AzVM`

**Exam tip:**

- Azure CLI → az ... → Bash
- Azure PowerShell → Get-Az..., New-Az... → PowerShell

Therefore, the solution does not meet the goal because the command is an Azure CLI command and the proposed Cloud Shell environment is PowerShell.

---

## 96. An Azure administrator plans to run a PowerShell script that creates Azure resources. You need to recommend which computer configuration to use to run the script. Solution: Run the script from a computer that runs Windows 10 and has the Azure PowerShell module installed. Does this meet the goal?

- Yes.
- No.

**Answer:** Yes.

**Explanation:** The solution meets the goal because **Windows 10** can run **PowerShell**, and installing the **Azure PowerShell module** provides the required cmdlets to create and manage Azure resources.

The administrator can execute the PowerShell script directly from the Windows 10 computer.

**Why the answer is Yes:**

- **Windows 10** → Supports PowerShell.
- **Azure PowerShell module** → Provides PowerShell cmdlets for managing Azure resources.
- **PowerShell script** → Can be executed locally on the Windows 10 computer.

**Exam tip:**

- **PowerShell script + Azure PowerShell module** → Valid configuration.
- **Azure CLI command (`az ...`)** → Use Azure CLI.
- **PowerShell script** → Requires a PowerShell environment.

Therefore, the solution **meets the goal**.

**Answer: Yes.**

---

## 97. Which service provides serverless computing in Azure?

- Azure Virtual Machines.
- Azure Functions.
- Azure storage account.
- Azure Container Instances.

**Answer:** Azure Functions.

**Explanation:** **Azure Functions** is an Azure **serverless computing** service that allows you to run code without managing the underlying servers or infrastructure. You pay primarily based on execution and resource consumption, depending on the hosting plan.

**Why the others are incorrect:**

- **Azure Virtual Machines** — An **IaaS** service where you are responsible for managing the operating system and much of the infrastructure.
- **Azure Storage account** — Provides storage services such as Blob, Files, Queues, and Tables; it is not a serverless compute service.
- **Azure Container Instances** — Runs containers without requiring you to manage virtual machines, but it is a **container service**, not the standard serverless compute service represented by Azure Functions in this question.

**Exam tip:**  
**Serverless compute → Azure Functions**

---

## 98. You have an Azure subscription named Subscription1. You sign in to the Azure portal and create a resource group named RG1. From Azure documentation, you have the following command that creates a virtual machine named VM1. `az vm create --resource-group RG1 --name VM1 --image UbuntuLTS --generate-ssh-keys`. You need to create VM1 in Subscription1 by using the command. Solution: From the Azure portal, launch Azure Cloud Shell and select Bash. Run the command in Cloud Shell. Does this meet the goal?

- Yes.
- No.

**Answer:** Yes.

**Explanation:** The solution meets the goal because the command provided is an **Azure CLI** command, and **Bash in Azure Cloud Shell** supports Azure CLI commands.

When Azure Cloud Shell is launched from the Azure portal, it is automatically authenticated to the Azure account. The command can therefore be used to create VM1 in the subscription containing the resource group RG1.

**Command:**

```bash
az vm create --resource-group RG1 --name VM1 --image UbuntuLTS --generate-ssh-keys
```

**Why the answer is Yes:**

- az vm create → Azure CLI command
- Bash → Supports Azure CLI commands.
- Azure Cloud Shell → Provides a preconfigured Azure CLI environment.
- RG1 → Already exists in Subscription1.
- The VM will be created in the subscription containing RG1.

**Exam tip:**

- az ... → Azure CLI → Bash
- New-AzVM, Get-AzVM, etc. → Azure PowerShell → PowerShell

Therefore, the solution meets the goal.

**Answer:** Yes.

---

## 99. Your company has several business units. Each business unit requires 20 different Azure resources for daily operation. All the business units require the same type of Azure resources. You need to recommend a solution to automate the creation of the Azure resources. What should you include in the recommendations?

- Azure Resource Manager templates.
- Virtual machine scale sets.
- The Azure API Management service.
- Management groups.

**Answer:** Azure Resource Manager templates.

**Explanation:** **Azure Resource Manager (ARM) templates** allow you to define Azure resources and their configurations in a declarative template. The same template can then be used repeatedly to deploy an identical set of resources for each business unit.

This is ideal when multiple business units require the **same types of Azure resources** and you want to automate and standardize their deployment.

**Why the others are incorrect:**

- **Virtual machine scale sets** — Used to deploy and manage a group of identical virtual machines and automatically scale the number of VM instances. They are not designed to deploy multiple types of Azure resources.
- **Azure API Management** — Used to publish, secure, manage, and monitor APIs. It does not automate the creation of general Azure resources.
- **Management groups** — Used to organize and manage multiple Azure subscriptions and apply governance policies. They do not automate resource deployment.

**Exam tip:**

**Automated and repeatable deployment of multiple Azure resources → ARM templates**

---

## 100. Which Azure service should you use to collect events from multiple resources into a centralized repository?

- Azure Event Hubs.
- Azure Analysis Services.
- Azure Monitor.
- Azure Stream Analytics.

**Answer:** Azure Event Hubs.

**Explanation:** **Azure Event Hubs** is a highly scalable event ingestion service designed to collect and process large volumes of events from multiple sources into a centralized location.

It is commonly used for **telemetry, application events, logs, and streaming data** from many resources and devices.

**Why the others are incorrect:**

- **Azure Analysis Services** — Used for enterprise-grade analytical models and data analysis, not primarily for collecting events.
- **Azure Monitor** — Collects and analyzes monitoring data, metrics, and logs from Azure resources, but the wording "collect events from multiple resources into a centralized repository" in this question specifically points to Event Hubs.
- **Azure Stream Analytics** — Processes and analyzes real-time streaming data; it is generally used after event data has been ingested rather than being the primary event ingestion service.

**Exam tip:**

**Many sources → Large volume of events → Centralized event ingestion → Azure Event Hubs**
