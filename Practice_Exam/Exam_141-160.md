## 141. Your company plans to migrate all its data and resources to Azure. The company migration plan states that only Platform as a Service (PaaS) solutions must be used in Azure. You need to deploy an Azure environment that meets the company migration plan. Solution: You create an Azure App Service and Azure virtual machines that have Microsoft SQL Server installed. Does this meet the goal?

- Yes.
- No.

**Answer:** No.

**Explanation:** The solution does not meet the requirement because **Azure Virtual Machines are Infrastructure as a Service (IaaS)**, not PaaS.

Although **Azure App Service** is a PaaS service, the virtual machines running Microsoft SQL Server are still IaaS resources. Therefore, the solution violates the requirement that **only PaaS solutions** must be used.

**Why the answer is No:**

- **Azure App Service** → PaaS
- **Azure Virtual Machines** → IaaS
- **SQL Server installed on an Azure VM** → IaaS

To meet the requirement, the company could use a PaaS database service such as **Azure SQL Database** instead of installing SQL Server on a virtual machine.

**Exam tip:**

**App Service → PaaS**

**Azure SQL Database → PaaS**

**Azure Virtual Machine → IaaS**

Therefore, the solution **does not meet the goal**.

**Answer: No.**

---


## 142. Your company hosts an accounting application named App1 that is used by all the customers of the company. App1 has low usage during the first three weeks of each month and very high usage during the last week of each month. Which benefit of Azure Cloud Services supports cost management for this type of usage pattern?

- High availability.
- High latency.
- Elasticity.
- Load balancing.

**Answer:** Elasticity.

**Explanation:** **Elasticity** allows cloud resources to automatically **scale up or down based on demand**.

In this scenario, App1 has:

- **Low usage** → During the first three weeks, fewer resources are needed.
- **Very high usage** → During the last week, additional resources can be provisioned.
- **After the peak period** → Resources can be reduced again to avoid unnecessary costs.

This helps the company **pay for the resources it actually needs** rather than maintaining enough infrastructure for the peak workload throughout the entire month.

**Why the others are incorrect:**

- **High availability** — Ensures services remain accessible despite failures; it does not directly address changing workload demand.
- **High latency** — Refers to delays in communication and would be undesirable.
- **Load balancing** — Distributes traffic across resources but does not automatically adjust the amount of resources based on demand.

**Exam tip:**

**Variable workload + automatically increase/decrease resources + cost optimization → Elasticity**

---

## 143. You plan to deploy several Azure virtual machines. You need to ensure that the services running on the virtual machines are available if a single data center fails. Solution: You deploy the virtual machines to two or more scale sets. Does this meet the goal?

- Yes.
- No.

**Answer:** No.

**Explanation:** **Virtual machine scale sets** allow you to create and manage a group of load-balanced VMs and can provide scalability, but simply deploying VMs to two or more scale sets does **not** guarantee protection against a single data center failure.

To protect against a **data center failure**, you should deploy the VMs across **multiple Availability Zones** within an Azure region.

**Why the answer is No:**

- **VM Scale Sets** → Primarily provide scalability and management of groups of VMs.
- **Availability Zones** → Physically separate data centers within an Azure region.
- Deploying VMs across multiple **Availability Zones** helps keep the application available if one data center/zone fails.

**Exam tip:**

**Protect against a data center failure → Availability Zones**

**Automatically scale/manage multiple VMs → VM Scale Sets**

Therefore, the solution **does not meet the goal**.

**Answer: No.**

---

## 144. You plan to provision Infrastructure as a Service (IaaS) resources in Azure. Which resource is an example of IaaS?

- An Azure web app.
- An Azure virtual machine.
- An Azure logic app.
- An Azure SQL database.

**Answer:** An Azure virtual machine.

**Explanation:** **Azure Virtual Machines** are an example of **Infrastructure as a Service (IaaS)**. With a VM, you have control over the operating system, installed software, and much of the configuration of the virtualized server.

**Why the others are incorrect:**

- **Azure Web App** → PaaS
- **Azure Logic App** → PaaS/serverless workflow service
- **Azure SQL Database** → PaaS

**Exam tip:**

**IaaS → Virtual Machines**

**PaaS → App Service, Azure SQL Database**

Therefore, the answer is **An Azure virtual machine**.

---

## 145. A team of developers at your company plans to deploy, and then remove, 50 customized virtual machines each week. Thirty of the virtual machines run Windows Server 2016 and 20 of the virtual machines run Ubuntu Linux. You need to recommend which Azure service will minimize the administrative effort required to deploy and remove the virtual machines. What should you recommend?

- Azure Reserved Virtual Machines (VM) Instances.
- Azure virtual machine scale sets.
- Azure DevTest Labs.
- Microsoft Managed Desktop.

**Answer:** Azure DevTest Labs.

**Explanation:** **Azure DevTest Labs** is designed for development and testing environments. It helps developers quickly create and manage virtual machines while minimizing administrative effort.

In this scenario, the team needs to **frequently deploy and remove customized VMs** running both Windows and Linux. DevTest Labs is well suited for this type of temporary development/test workload.

**Why the others are incorrect:**

- **Azure Reserved VM Instances** — Designed primarily to reduce costs when you commit to using VMs for a long period. They are not intended for frequently creating and deleting VMs.
- **Azure VM Scale Sets** — Designed to deploy and automatically scale a group of **identical or similarly configured VMs**. They are not the best choice when developers need customized VMs with different operating systems and configurations.
- **Microsoft Managed Desktop** — Provides managed Windows devices for users; it is not designed for deploying temporary Azure virtual machines.

**Exam tip:**

**Development/testing + frequently create/delete VMs → Azure DevTest Labs**

**Long-term VM usage + cost savings → Reserved VM Instances**

**Identical VMs + automatic scaling → VM Scale Sets**

Therefore, the answer is **Azure DevTest Labs**.

---

## 146. One of the benefits of Azure Synapse Analytics is that high availability is built into the platform.

- No change is needed.
- Automatic scaling.
- Data compression.
- Versioning.

**Answer:** No change is needed.

**Explanation:** **High availability** is a benefit built into **Azure Synapse Analytics**. The service is designed as a cloud-based analytics platform with built-in capabilities that help maintain availability and reliability.

The statement is therefore already correct.

**Why the others are incorrect:**

- **Automatic scaling** — Some Azure services support automatic scaling, but this does not replace the stated benefit of high availability.
- **Data compression** — Compression can improve storage and query efficiency, but it is not the key benefit described in the question.
- **Versioning** — Versioning is associated with managing different versions of data, code, or resources, not the primary built-in availability capability of Synapse Analytics.

**Exam tip:**

**Azure Synapse Analytics → Built-in high availability and scalable analytics**

Therefore, **No change is needed**.

---

## 147. Match the Azure services to the correct descriptions.

- Provide operating system virtualization: Azure App Service. Provide portable environment for virtualized applications: Azure virtual machines. Used to build, deploy, and scale web apps: Azure Container Instances. Provide a platform for serverless code: Azure Functions.
- Provide operating system virtualization: Azure Functions. Provide portable environment for virtualized applications: Azure virtual machines. Used to build, deploy, and scale web apps: Azure Container Instances. Provide a platform for serverless code: Azure App Service.
- Provide operating system virtualization: Azure virtual machines. Provide portable environment for virtualized applications: Azure Container Instances. Used to build, deploy, and scale web apps: Azure App Service. Provide a platform for serverless code: Azure Functions.
- Provide operating system virtualization: Azure virtual machines. Provide portable environment for virtualized applications: Azure Functions. Used to build, deploy, and scale web apps: Azure App Service. Provide a platform for serverless code: Azure Container Instances.

**Answer:** The **third option**.

### Correct matching

| Description | Azure service |
|---|---|
| Operating system virtualization | **Azure Virtual Machines** |
| Portable environment for virtualized applications | **Azure Container Instances** |
| Build, deploy, and scale web apps | **Azure App Service** |
| Platform for serverless code | **Azure Functions** |

**Explanation:**

- **Azure Virtual Machines** → Provides virtualized servers where you control the operating system and software.
- **Azure Container Instances (ACI)** → Runs containerized applications without requiring you to manage the underlying VM infrastructure.
- **Azure App Service** → PaaS for building, deploying, and scaling web applications.
- **Azure Functions** → Serverless compute service for running event-driven code.

**Exam tip:**

**VM → Virtualized OS**

**ACI → Containers**

**App Service → Web apps**

**Functions → Serverless code**

**Answer: Option 3.**

---

## 148. An Azure administrator plans to run a PowerShell script that creates Azure resources. You need to recommend which computer configuration to use to run the script. Solution: Run the script from a computer that runs macOS and has PowerShell Core 6.0 installed. Does this meet the goal?

- Yes.
- No.

**Answer:** Yes.

**Explanation:** **PowerShell Core** is cross-platform and can run on **Windows, Linux, and macOS**. Therefore, a macOS computer with **PowerShell Core 6.0** installed can run PowerShell scripts.

The administrator can use the **Azure PowerShell modules** with PowerShell Core to authenticate to Azure and create Azure resources.

**Why the answer is Yes:**

- PowerShell Core supports **macOS**.
- Azure PowerShell can be used to manage Azure resources.
- PowerShell scripts can create and configure Azure resources.

**Exam tip:**

**Azure PowerShell + PowerShell Core → Windows, Linux, and macOS**

Therefore, the solution **meets the goal**.

**Answer: Yes.**

---

## 149. You have an Azure environment that contains 10 virtual networks and 100 virtual machines. You need to limit the amount of inbound traffic to all the Azure virtual networks. What should you create?

- One application security group (ASG).
- 10 virtual network gateways.
- 10 Azure ExpressRoute circuits.
- One Azure Firewall.

**Answer:** One Azure Firewall.

**Explanation:** **Azure Firewall** is a managed, centralized network security service that can control and filter inbound and outbound traffic. It can be deployed in a hub-and-spoke architecture to provide centralized traffic filtering for multiple virtual networks.

This makes it appropriate when you need to control traffic across **multiple Azure virtual networks**.

**Why the others are incorrect:**

- **One application security group (ASG)** — ASGs are used with NSGs to group VMs based on application roles and simplify traffic rules. An ASG does not provide centralized filtering for multiple virtual networks.
- **10 virtual network gateways** — VPN/virtual network gateways provide connectivity between networks, such as Azure and on-premises networks. They are not primarily used for centralized traffic filtering.
- **10 Azure ExpressRoute circuits** — ExpressRoute provides private connectivity between on-premises networks and Azure; it is not a traffic-filtering solution.

**Exam tip:**

**Centralized traffic filtering across multiple VNets → Azure Firewall**

**VM/subnet-level traffic filtering → NSG**

**Group VMs for NSG rules → ASG**

**Private connectivity to Azure → VPN Gateway / ExpressRoute**

Therefore, the answer is **One Azure Firewall**.

---

## 150. You have an on-premises application that sends email notifications automatically based on a rule. You plan to migrate the application to Azure. You need to recommend a serverless computing solution for the application. What should you include in the recommendation?

- A web app.
- A server image in Azure Marketplace.
- A Logic App.
- An API app.

**Answer:** A Logic App.

**Explanation:** **Azure Logic Apps** is a serverless workflow service that allows you to automate processes based on triggers and conditions. It is well suited for an application that needs to **automatically send email notifications when a specific rule or event occurs**.

Logic Apps provides built-in connectors for services such as email, Microsoft 365, Azure services, databases, and many other systems.

**Why the others are incorrect:**

- **A web app** — Used to host web applications and APIs; it is not the best choice for rule-based serverless workflows.
- **A server image in Azure Marketplace** — Would require a server/VM environment and therefore is not a serverless solution.
- **An API app** — Used to host APIs but is not specifically designed for workflow automation.

**Exam tip:**

**Automated workflow + trigger/rule + email notification → Azure Logic Apps**

**Serverless code execution → Azure Functions**

Therefore, the answer is **A Logic App**.

---

## 151. You have an application that is comprised of an Azure web app that has a Service Level Agreement (SLA) of 99.95 percent and an Azure SQL database that has an SLA of 99.99 percent. The composite SLA for the application is the product of both SLAs, which equals 99.94 percent.

- No change is needed.
- The lowest SLA associated to the application, which is 99.95 percent.
- The highest SLA associated to the application, which is 99.99 percent.
- The difference between the two SLAs, which is 0.05 percent.

**Answer:** No change is needed.

**Explanation:** When an application depends on multiple Azure services, the **composite SLA** is calculated by multiplying the individual SLA percentages.

For this application:

- Azure Web App = **99.95%**
- Azure SQL Database = **99.99%**

Composite SLA:

**99.95% × 99.99% = 99.940005%**

Rounded to two decimal places:

**99.94%**

Therefore, the statement is correct.

**Exam tip:**

**Composite SLA for dependent services → Multiply the individual SLAs**

Example:

**99.95% × 99.99% ≈ 99.94%**

Therefore, the answer is **No change is needed**.

---

## 152. You have an Azure subscription named Subscription1. You sign in to the Azure portal and create a resource group named RG1. From Azure documentation, you have the following command that creates a virtual machine named VM1.

`az vm create --resource-group RG1 --name VM1 --image UbuntuLTS --generate-ssh-keys`

You need to create VM1 in Subscription1 by using the command.

**Solution:** From a computer that runs Windows 10, install Azure CLI. From PowerShell, sign in to Azure and then run the command. Does this meet the goal?

- Yes.
- No.

**Answer:** Yes.

**Explanation:** **Azure CLI** is a cross-platform command-line tool that can be installed on **Windows 10** and run from **PowerShell**.

After signing in with Azure CLI, the command:

`az vm create --resource-group RG1 --name VM1 --image UbuntuLTS --generate-ssh-keys`

can create VM1 in the subscription associated with the current Azure CLI session.

Because **RG1 already exists in Subscription1**, the command can create VM1 in that resource group.

**Why the answer is Yes:**

- Windows 10 supports **Azure CLI**.
- Azure CLI can be run from **PowerShell**.
- `az login` authenticates the user to Azure.
- The VM creation command can then be executed using Azure CLI.
- The resource group `RG1` identifies where VM1 will be created.

**Exam tip:**

**Azure CLI → Windows, Linux, macOS**

**PowerShell → Can run Azure CLI commands**

**`az login` → Authenticate to Azure**

Therefore, the solution **meets the goal**.

**Answer: Yes.**

---

## 153. You have an Azure subscription named Subscription1. You sign in to the Azure portal and create a resource group named RG1. From Azure documentation, you have the following command that creates a virtual machine named VM1.

`az vm create --resource-group RG1 --name VM1 --image UbuntuLTS --generate-ssh-keys`

You need to create VM1 in Subscription1 by using the command.

**Solution:** From a computer that runs Windows 10, install Azure CLI. From a command prompt, sign in to Azure and then run the command. Does this meet the goal?

- Yes.
- No.

**Answer:** Yes.

**Explanation:** **Azure CLI** can be installed and used on **Windows 10**, and it can be executed from a standard **Command Prompt**.

After authenticating with Azure CLI, you can execute the `az vm create` command to create the virtual machine.

The command:

`az vm create --resource-group RG1 --name VM1 --image UbuntuLTS --generate-ssh-keys`

will create VM1 in the resource group `RG1`, which was created in **Subscription1**.

**Why the answer is Yes:**

- Azure CLI supports **Windows**.
- Azure CLI can run from **Command Prompt**.
- You can authenticate using `az login`.
- Azure CLI commands such as `az vm create` can be executed from Command Prompt.
- Since RG1 belongs to Subscription1, VM1 can be created there.

**Exam tip:**

**Azure CLI → Command Prompt / PowerShell / Cloud Shell**

**Azure CLI authentication → `az login`**

Therefore, the solution **meets the goal**.

**Answer: Yes.**

---

## 154. You have an Azure environment. You need to create a new Azure virtual machine from a tablet that runs the Android operating system. Solution: You use PowerShell in Azure Cloud Shell. Does this meet the goal?

- Yes.
- No.

**Answer:** Yes.

**Explanation:** **Azure Cloud Shell** is a browser-based, interactive shell that allows you to manage Azure resources without installing Azure management tools on the local device.

Because Azure Cloud Shell is accessed through a web browser, it can be used from a **tablet running Android**. Cloud Shell provides both **PowerShell** and **Bash** environments.

Therefore, you can use **PowerShell in Azure Cloud Shell** to create an Azure virtual machine from the Android tablet.

**Why the answer is Yes:**

- Azure Cloud Shell is browser-based.
- It can be accessed from Android devices through a browser.
- It provides **PowerShell** and **Bash**.
- Azure resources can be created and managed directly from Cloud Shell.

**Exam tip:**

**Android/tablet + Azure management → Azure Portal or Azure Cloud Shell**

**Cloud Shell → PowerShell + Bash**

Therefore, the solution **meets the goal**.

**Answer: Yes.**

---

## 155. Azure Databricks is an Apache Spark-based analytics service.

- No change is needed.
- Azure Data Factory.
- Azure DevOps.
- Azure HDInsight.

**Answer:** No change is needed.

**Explanation:** **Azure Databricks** is a cloud-based analytics platform built around **Apache Spark**. It provides a collaborative environment for data engineering, data science, machine learning, and analytics.

**Why the others are incorrect:**

- **Azure Data Factory** — A cloud-based data integration and ETL/ELT service used to move and transform data.
- **Azure DevOps** — Provides tools for software development, source control, CI/CD, and project management.
- **Azure HDInsight** — A managed open-source analytics service that supports frameworks such as Apache Hadoop and Apache Spark, but it is not the service described in the statement.

**Exam tip:**

**Apache Spark-based analytics platform → Azure Databricks**

Therefore, **No change is needed**.

---

## 156. Azure Site Recovery provides fault tolerance for virtual machines.

- No change is needed.
- Disaster recovery.
- Elasticity.
- High availability.

**Answer:** Disaster recovery.

**Explanation:** **Azure Site Recovery (ASR)** is a disaster recovery service that helps keep applications running during major outages by **replicating workloads** and enabling them to be recovered in a secondary location.

Its primary purpose is **disaster recovery**, rather than providing continuous fault tolerance or high availability.

**Why the others are incorrect:**

- **Fault tolerance** — Refers to a system continuing to operate despite component failures. ASR focuses on recovering workloads after a major outage.
- **Elasticity** — Refers to automatically increasing or decreasing resources based on demand.
- **High availability** — Focuses on keeping services continuously available, typically through redundancy and distributed deployment.

**Exam tip:**

**Azure Site Recovery → Disaster recovery**

**Availability Zones → High availability / protection from data center failures**

**Autoscaling → Elasticity**

Therefore, the correct answer is **Disaster recovery**.

---

## 157. An Availability Zone in Azure has physically separate locations across two continents.

- No change is needed.
- Within a single Azure region.
- Within multiple Azure regions.
- Within a single Azure datacenter.

**Answer:** Within a single Azure region.

**Explanation:** **Azure Availability Zones** are physically separate locations **within a single Azure region**. Each zone consists of one or more data centers with independent power, cooling, and networking.

They are designed to protect applications from failures affecting a single data center while keeping resources within the same Azure region.

**Why the others are incorrect:**

- **Across two continents** — This describes a geographic separation much larger than an Availability Zone.
- **Within multiple Azure regions** — This refers to using multiple regions, not Availability Zones.
- **Within a single Azure datacenter** — An Availability Zone is designed to provide separation between data centers, not within the same data center.

**Exam tip:**

**Availability Zone → Physically separate locations within ONE Azure region**

**Azure Region → One or more data centers connected by a low-latency network**

Therefore, the answer is **Within a single Azure region**.

---

## 158. Your company plans to migrate all on-premises data to Azure. You need to identify whether Azure complies with the company regional requirements. What should you use?

- The Knowledge Center.
- Azure Marketplace.
- The MyApps portal.
- The Trust Center.

**Answer:** The Trust Center.

**Explanation:** The **Microsoft Trust Center** provides information about Microsoft's **security, privacy, compliance, and regulatory practices**, including information about Azure compliance with various regional and industry-specific requirements.

It can help organizations determine whether Microsoft cloud services meet their **regional, legal, regulatory, and compliance requirements**.

**Why the others are incorrect:**

- **Knowledge Center** — Provides documentation and information for Microsoft products but is not the primary source for compliance and regulatory information.
- **Azure Marketplace** — Used to discover and purchase Azure solutions and applications from Microsoft and partners.
- **MyApps portal** — Used by users to access applications made available through Microsoft Entra ID.

**Exam tip:**

**Azure compliance, privacy, security, and regional requirements → Trust Center**

Therefore, the answer is **The Trust Center**.

---

## 159. Azure Key Vault is used to store secrets for Microsoft Entra ID user accounts.

- No change is needed.
- Microsoft Entra ID administrative accounts.
- Personally Identifiable Information (PII).
- Server applications.

**Answer:** Server applications.

**Explanation:** **Azure Key Vault** is designed to securely store and manage **secrets, keys, and certificates** used by applications and services.

For example, a server application can use Key Vault to securely retrieve:

- API keys
- Passwords
- Connection strings
- Encryption keys
- Certificates

This prevents sensitive credentials from being stored directly in application code or configuration files.

**Why the others are incorrect:**

- **Microsoft Entra ID user accounts** — Key Vault is not used as the primary storage for user account credentials.
- **Microsoft Entra ID administrative accounts** — Administrative account credentials are managed through Microsoft Entra ID and related identity/security mechanisms.
- **Personally Identifiable Information (PII)** — Key Vault is designed for secrets, keys, and certificates rather than general-purpose PII storage.

**Exam tip:**

**Azure Key Vault → Secrets + Keys + Certificates**

**Application secrets → Store securely in Key Vault**

Therefore, the answer is **Server applications**.

---

## 160. After you create a virtual machine, you need to modify the Network Security Group (NSG) to allow connections to TCP port 8080 on the virtual machine.

- No change is needed.
- Virtual network gateway.
- Virtual network.
- Route table.

**Answer:** No change is needed.

**Explanation:** An **Azure Network Security Group (NSG)** is used to control **inbound and outbound network traffic** to Azure resources such as virtual machines.

To allow connections to **TCP port 8080**, you can add an **inbound security rule** to the NSG associated with the VM's network interface or subnet.

For example:

- **Source:** Internet (or a specific source)
- **Destination:** VM
- **Protocol:** TCP
- **Destination port:** 8080
- **Action:** Allow

**Why the others are incorrect:**

- **Virtual network gateway** — Provides connectivity between networks, such as Azure and an on-premises network.
- **Virtual network** — Provides the network boundary for Azure resources but does not directly define port-level traffic rules.
- **Route table** — Controls how network traffic is routed, not whether a specific TCP port is allowed or denied.

**Exam tip:**

**Allow/deny specific ports → NSG**

**Connect networks → VPN/Virtual Network Gateway**

**Determine traffic paths → Route Table**

Therefore, **No change is needed**.
